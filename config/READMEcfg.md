# /config

_This folder is reserved for future configuration files (e.g., hyperparameters). No config files were used in TP02._

This folder may contains configuration files in the future to manage parameters, settings, and environment details for the Optimotion project.

## Intended Contents:
- YAML or JSON files with hyperparameters (e.g., link lengths, learning rates, iteration counts)
- Setup files for simulations if stretch goals (animation) are pursued

## Example Future Files:
- `default_config.yaml`
- `simulation_params.json`

## Acknowledgments
OpenAI. (2025). *ChatGPT’s assistance with mathematical modeling and code generation for DS623 Optimotion project* [Large language model]. https://openai.com/chatgpt